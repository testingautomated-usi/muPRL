import os

SENSITIVITY_MS = "KR"  # KR - MS based on killability rates (proportion of killed pairs) alternative is the proportion of killed configs (TR)
RESULTS_ROOT = "mutation_analysis_results"
ROOT_DIR = os.path.dirname(os.path.abspath(__file__))
